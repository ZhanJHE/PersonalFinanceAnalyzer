<Window x:Class="PersonalFinanceAnalyzer.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="登录 / 注册" Height="420" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="个人收支趋势分析器" FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,15"
                   Foreground="#1976D2"/>

        <!-- Server URL -->
        <TextBlock Grid.Row="1" Text="服务器地址" FontSize="13" Margin="0,0,0,3"/>
        <TextBox x:Name="ServerUrlBox" Grid.Row="2" FontSize="13" Padding="6"
                 Height="30" Margin="0,0,0,8"/>

        <!-- Username -->
        <TextBlock Grid.Row="3" Text="用户名" FontSize="14" Margin="0,0,0,3"/>
        <TextBox x:Name="UsernameBox" Grid.Row="4" FontSize="14" Padding="8"
                 Height="36" Margin="0,0,0,8"/>

        <!-- Password -->
        <TextBlock Grid.Row="5" Text="密码" FontSize="14" Margin="0,0,0,3"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="6" FontSize="14" Padding="8"
                     Height="36" Margin="0,0,0,8"/>

        <!-- Error Message -->
        <TextBlock x:Name="ErrorText" Grid.Row="7" Foreground="#D32F2F"
                   FontSize="13" Margin="0,0,0,8" TextWrapping="Wrap"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="LoginButton" Content="登录"
                    Click="OnLoginClick" Background="#1976D2" Foreground="White"
                    Padding="30,10" FontSize="14" FontWeight="Bold" Margin="0,0,10,0"/>
            <Button x:Name="RegisterButton" Content="注册"
                    Click="OnRegisterClick" Background="#388E3C" Foreground="White"
                    Padding="30,10" FontSize="14" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
